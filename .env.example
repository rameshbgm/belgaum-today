# ═══════════════════════════════════════════════════
# Belgaum Today — Environment Configuration
# ═══════════════════════════════════════════════════
# Copy this file to .env.local and fill in the values.
# Only variables without defaults MUST be configured.

# ── Database Configuration (MySQL) ──
DATABASE_HOST=127.0.0.1
DATABASE_PORT=3307
DATABASE_USER=belgaum_user
DATABASE_PASSWORD=belgaum_pass
DATABASE_NAME=belgaum_today

# ── JWT Authentication ──
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# ── Site Configuration ──
SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=Belgaum Today

# ── RSS Feed Configuration ──
RSS_FETCH_INTERVAL_MINUTES=120
CRON_SECRET=belgaum-today-cron-secret-2026

# ── AI LLM Provider API Keys ──
# The active provider is configured in the admin panel (ai_providers table).
# API keys can be stored encrypted in the database (ai_api_keys table)
# OR provided here as .env fallback. Only the active provider's key is needed.
#
# Supported providers:
#   OpenAI     → gpt-4o-mini, chatgpt-4o-latest (https://platform.openai.com/api-keys)
#   Anthropic  → claude-3-5-sonnet, claude-3-5-haiku (https://console.anthropic.com/)
#   DeepSeek   → deepseek-chat, deepseek-reasoner (https://platform.deepseek.com/)
#   Google     → gemini-2.0-flash, gemini-1.5-pro (https://aistudio.google.com/apikey)
#   SarvamAI   → sarvam-m (https://dashboard.sarvam.ai/)

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=
DEEPSEEK_API_KEY=
SARVAM_API_KEY=

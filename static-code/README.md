# Static Site Backup

This directory holds the static site backup that should live on the `static-code` branch.

To recreate the backup branch, copy the contents of this folder to the repository root on a
branch named `static-code` and publish it.
